# models/schema.yml

version: 2

sources:
  - name: public
    database: exnaton_test
    schema: public
    tables:
      - name: muid_2

models:
  - name: muid_2_transforms
    description: "Extract JSONB array elements into a table"
    columns:
      - name: tags
        description: "tags from JSONB element"
      - name: measurement
        description: "measurement from JSONB element"
      - name: timestamp
        description: "timestamp from JSONB element"
      - name: 0100021D00FF
        description: "0100021D00FF from JSONB element"  